export type GetErrorResponseType = {
    displayMode: string,
    uiLocales: string,
    error: string,
    errorDescription: string | null,
    requestId: string,
    redirectUri: string,
    responseMode: string,
    clientId: string
};